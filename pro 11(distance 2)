#include<stdio.h>
int main()
{
    double x1,x2,y1,y2,distance;

    x1=25;
    x2=35;
    y1=15;
    y2=10;

     distance=sqrt(pow((x2-x1),2)+pow((y2-y1),2));
     printf("distance=%.4lf",distance);
     return 0;

}
